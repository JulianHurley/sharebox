-if @parent_folder
	%li= link_to 'Upload File', new_sub_asset_path(parent_id: @parent_folder.id) 
	SUB
	%li= link_to 'New Folder', new_sub_folder_path(parent_id: @parent_folder.id)
-else
	%li= link_to 'Upload File', new_asset_path(@parent_folder)
	%li= link_to 'New Folder', new_folder_path